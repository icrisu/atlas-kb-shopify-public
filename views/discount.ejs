<!doctype html>
<html class="no-js" lang="">

<head>
    <meta charset="utf-8">
    <meta http-equiv="x-ua-compatible" content="ie=edge">
    <title><%= appNamePretty %> - discount</title>
    <meta name="description" content="">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <style>
        html, body {
            margin: 0; padding: 20px 0px;
            background: #efeeef;
            font-family: 'Roboto', sans-serif;
        }
        @media only screen and (max-width: 700px) {
            html, body {
                padding: 20px 10px;
            }
        }
        .container {
            display: flex;
            justify-content: center;
            padding-top: 5%;
        }
        .content {
            max-width: 600px;
        }
        @media only screen and (max-width: 700px) {
            .content {
                width: 100%;
            }
        }
        .app-name {
            font-size: 24px;
            margin-bottom: 20px;
            text-align: center;
        }
        @media only screen and (max-width: 700px) {
            .app-name {
                font-size: 20px;
            }
        }
        .gift-code {
            font-size: 18px;
            font-weight: 500;
            text-align: center;
            margin-bottom: 40px;
            line-height: 150%;
        }
        .perks-list {
            margin: 0; padding: 0; list-style-type: none;
            margin-bottom: 35px;
        }
        .perks-list li {
            margin-bottom: 5px;
        }
        .perk-img {
            max-width: 40px;
            float: left;
            margin-right: 15px;
            margin-bottom: 10px;
        }
        .perk-description {
            font-size: 14px;
            color: #696969;
            line-height: 140%;
        }
        #shop {
            outline: none;
            padding: 12px 10px;
            font-size: 15px;
            display: block;
            width: 100%;
            box-sizing : border-box;
            border: solid 1px #dddddd;
            border-radius: 4px;
        }
        input[type=submit] {
            padding: 12px 10px; 
            background: #5565bf;
            border-radius: 4px;
            border:0 none;
            color: #f4f4f4;
            cursor:pointer;
            -webkit-border-radius: 4px;
            display: block;
            font-family: 'Roboto', sans-serif;
            font-weight: 500;
            outline: none;
            margin-top: 15px;
            width: 100%;
            font-size: 16px;
            text-align: center;
        }
        input[type=submit]:hover {
            background: #5e6ec6;
        }
        .clearfix:after {
        visibility: hidden;
        display: block;
        font-size: 0;
        content: " ";
        clear: both;
        height: 0;
        }
        .clearfix { display: inline-block; }
        /* start commented backslash hack \*/
        * html .clearfix { height: 1%; }
        .clearfix { display: block; }
        /* close commented backslash hack */
a:link {
  color: #5e6dc1;
}
a:visited {
  color: #5e6dc1;
}
a:hover {
  color: #5e6dc1;
}
a:active {
  color: #5e6dc1;
}
    </style>
    <link href="https://fonts.googleapis.com/css?family=Roboto:300i,400,500" rel="stylesheet">
    <script src="/js/jquery3.3.1.js" type="text/javascript"></script>
    <script type="text/javascript">
        // var adminLocalStorageKey = "admin_storage_imp_s";
        window.appHost = "<%= host %>";
        jQuery( document ).ready(function() {
            jQuery('#action-form').on('submit', function(event) {
                event.preventDefault();
                window.location.replace(window.appHost + '/install-shop/auth?shop=' + jQuery('#shop').val() + '&perks=' + jQuery('#perks').val());
            });
        });
    </script>
</head>

<body>
    <div class="container">
        <div class="content">
            <a href="https://apps.shopify.com/atlas-help-center" target="_blank"><h1 class="app-name"><%= appNamePretty %></h1></a>
            <% if (perks && perks.length > 0) { %>
                <p class="gift-code">Your gift is ready!<br /><span><%= code %></span></p>
                <ul class="perks-list">
                    <% perks.forEach(function(perk){ %>
                        <li>
                            <img class="perk-img" src="/discount/img/gift.png" alt="gift" />
                            <div class="perk-description"><%- perk.description %></div>
                            <div class="clearfix"></div>
                        </li>
                    <% }); %>
                </ul>
                <form id="action-form" action="" method="get">
                    <input id="perks" type="hidden" name="perks" value="<%= code %>">
                    <input class="shop-input" id="shop" type="text" required minlength="15" name="shop" placeholder="your-shop.myshopify.com" value="">
                    <input type="submit" value="Install app">
                </form> 
            <% } else{ %>  
                <p>Hmm :( ... it seems that the discount code is not valid.</p>
            <% } %>
        </div>
    </div>
</body>

</html>